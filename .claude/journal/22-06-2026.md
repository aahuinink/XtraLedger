# Week of 2026-06-22

## 2026-06-22 (Mon)

**Work done:**
- ADR 0002 written and committed (`95ecb89`) — drops `generation`, drops `tags` byte, adds
  `normality` enum column, establishes desc string pool direction, geometric grow-on-demand capacity.
- `architecture.md` §4 updated: account closure job removed (`0d34b85`).
- Homework Q1–Q3 committed (`d5c7870`).

No production code written. HANDOFF updated after a mid-session design discussion (2026-06-21 Opus).

---

## 2026-06-24 (Wed)

**Mentoring model:** Opus started the session, switched to Sonnet mid-session.

**Work done:**
- Homework Q4–Q5 answered (uncommitted, sitting in working tree).
- `account.h` redesigned: added `xl_account_store` forward decl, added `xl_acct_normality` enum,
  switched raw `char[]` arrays to `xl_smallstr64`/`xl_smallstr128`, added `store` param to all
  accessors, improved doc comments throughout. **Still has the Q4 bug**: `const` members remain
  on `xl_account_snapshot` fields — fix not landed yet.
- `xl_account_view.norm` is still a value, not a pointer — inconsistent with siblings.
- Resize-survival test added to `tests/internal/lookup-table.c` — failing as expected (the
  test is correct; the implementation is wrong).
- Partial attempt at the `calloc` switch in `increase_capacity_if_needed`: `malloc` → `calloc`,
  `memcpy` removed, but the rehash reinsert was not implemented. Left as `// iterate over all entries and re-index` comment + old entry array free removed.

**Decision made:** rewrite `arena-lookup-table` rather than patch. Too many stacked bugs.

**Bugs found in `arena-lookup-table.c` (from Opus review, `feedback/24-06-2026.md`):**
1. `memcpy(table->entries, new_entry_array, table->capacity)` — wrong arg order (src/dest swapped),
   wrong byte count (should be `* sizeof(entry)`), src gets freed immediately after.
2. `table->capacity` stomped to new size before copy — can't correctly bound the old data copy.
3. Rehash never happens — `populate_lookup_table` only inserts new keys; existing entries keep
   stale indices after a grow. This is the Q1 bug.
4. UB sentinel `0xF364` / `malloc` + read of uninitialized memory. Q3 bug.
5. `arena_lookup_try_get` `while(true)` probe loop has no capacity bound — can loop forever.
6. Dead prototype `get_starting_entry` declared, never defined or used.

**Mistakes / things to watch:**
- Q4 reasoning was correct but the fix wasn't applied to the header. "I understand it" ≠ "it's done."
- The partial `calloc` patch left the implementation in a worse-than-before state (old entries freed
  without being reinserted anywhere). Good instinct to stop and rewrite rather than keep patching.
- Q5 answer has a typo: "ohter" and references a non-existent file path
  (`src/xlcore/memory/lookup-tabe.c` — missing an 'l').

---

## 2026-06-25 (Thu)

**Work done:**
- Working tree committed as clean baseline (`fd4da54`): homework Q4–Q5, `account.h`
  redesign, partial calloc patch, resize-survival test.
- Began rewriting `arena-lookup-table.c`. Wrote `resize_table_if_needed` (replaces
  `increase_capacity_if_needed`): correct structure — overflow check, capacity calc,
  early return, calloc, null check, NULL-entries init path, rehash loop, free old,
  update table. **Uncommitted. In progress.**
- Design decision locked: **`hash == 0` as empty sentinel** — `state` field will be
  removed from `arena_lookup_entry`. `hash()` will guarantee non-zero return
  (`return hash ? hash : 1`). TODO comment added to hash prototype.

**Bugs found / mistakes made this session:**
- Used `entry->hash != 0x00` as the empty check in the rehash loop — same sentinel
  confusion as yesterday, now resolved by the `hash == 0` decision.
- **Missing linear probe in rehash loop** (line 88): `new_entries[new_index] = *entry`
  blindly copies without checking if the slot is occupied. Two entries can collide at
  the same new index. Fix: probe for the next empty slot before copying.
- Unnecessary C++ cast on `calloc` (line 68): `(struct arena_lookup_entry *)calloc(...)`.
  Drop it in C.
- Overflow check wrong (line 53): `table->capacity >= (UINT16_MAX - num_new_keys)` tests
  capacity, not size. Should be `table->size + num_new_keys > UINT16_MAX`.
- Added unused declarations `lookup_table_get` / `lookup_table_put` (lines 22–25). Remove them.
- `get_starting_entry` dead prototype still present (line 38). Still not removed.

**Pattern to note:** Three sessions in a row where the right answer was stated in homework
but not applied in code. Q4 const fix explained correctly on Wed; still not in the header.
`state`-as-sentinel issue caught Monday, explained again Tuesday, still landed wrong today.
Understanding a fix and writing it are separate steps — close the loop immediately.

**Q1–Q5 homework quality (informal, Opus grades formally on weekend):**
- Q1: correct diagnosis and concrete hash example. Algorithm described correctly. Side note about
  tree-based hash is premature — park it.
- Q2: invariant phrased correctly. Assertions described correctly. "Crash-free ≠ correct" argument
  is solid.
- Q3: UB argument is correct. `calloc` cost point is partially correct — the real nuance is that
  `calloc` can be faster than `malloc+memset` because the OS provides pre-zeroed pages, but
  "small arrays" isn't quite the right axis; it's about whether pages are fresh from the OS or
  recycled from the process heap.
- Q4: diagnosis correct, fix shown correctly in the answer — but the header still has the bug.
- Q5: experiment design is reasonable. Would be stronger if it named a concrete measurement tool
  (perf, gprof, rdtsc) rather than just "time each loop."
