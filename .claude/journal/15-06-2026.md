# Week of 2026-06-15 — Code Review & Feedback

Reviewer: Opus (senior mentor) · Date written: 2026-06-20 (Sat)

## What you shipped this week

- `src/xlcore/memory/arena-lookup-table.{c,h}` — an open-addressing (linear-probing)
  string→arena-slot hash map keyed on `xl_smallstr64`. DJB2 hash, case-insensitive,
  power-of-two capacity, 0.7 max load factor.
- Test refactor: split `arena_tests.c` into `arena.c` + `lookup-table.c`, added a shared
  `debug_settings.h`, simplified `tests/CMakeLists.txt`.
- `account.h` API redesign (uncommitted): threaded an explicit `struct xl_account_store *`
  through every accessor, swapped raw `char[N]` buffers for `xl_smallstr64/128`, added
  `xl_acct_normality`, normalized doc comments (`@ret`→`@return`, added ERRORS sections).

Good instinct overall: SoA arena + a separate index structure is exactly the
data-oriented split a real ledger wants. The handle/snapshot/view triad is a thoughtful
ownership model. Below is where the rubber meets the road.

---

## Critical bugs (these are why I want you to re-open the resize path)

### 1. Resize never rehashes — the table is silently corrupt after the first grow
`populate_lookup_table()` only inserts the `num_new_keys` newest keys, starting at
`table->size`. But every existing entry's slot was computed as `hash & (old_capacity-1)`.
When capacity changes (8→16), `hash_to_index` returns *different* indices, so every
previously-inserted key is now in the wrong bucket. Open addressing requires you to
**reinsert every entry** on resize.

Your test passes only because it never re-looks-up the first 5 keys *after* the grow to 16
(`lookup-table.c:55-58` checks `capacity`/`size`, not retrieval). Add this and watch it fail:
```c
for (uint_fast8_t i = 0; i < 8; ++i)
    assert(arena_lookup_try_get(&table, &test_keys[i]) == i);   // will break for old keys
```
This is the single most important lesson of the week: **a green test suite is only as
strong as its assertions.** A passing test that doesn't assert the property you care about
is worse than no test, because it buys false confidence.

### 2. The resize `memcpy` is broken three independent ways (`arena-lookup-table.c:225`)
```c
memcpy(table->entries, new_entry_array, table->capacity);
```
- **src/dest swapped:** this copies the *uninitialized new* array *into the old* array,
  then you set `entries = new_entry_array` (still uninitialized) and free the old one. The
  old data is thrown away; the new array is never populated.
- **wrong length:** `table->capacity` is an element count (~16), used here as a *byte*
  count, and it's the *new* capacity, not the old occupied size. You'd want
  `old_count * sizeof(struct arena_lookup_entry)` at most.
- **wrong operation:** even a correct copy is invalid for a hash table — see bug #1, you
  must rehash, not blit. Once you rehash, this memcpy disappears entirely.

### 3. Reading uninitialized heap memory is UB, not just "unlikely" (`:164-165`, `:210`)
`malloc` returns indeterminate bytes; you then read `.state` and compare against
`ENTRY_STATE_ACTIVE`. Your comment frames the `0xF364` sentinel as "highly unlikely to
collide" — but the real problem isn't collision odds, it's that *reading indeterminate
memory is undefined behavior*, full stop. Compilers are allowed to assume it never happens.
Fix: `calloc` (or `memset` after `malloc`) and make `state == 0` mean "empty". Then the
sentinel-collision worry evaporates because you control the bytes. Cheaper and correct.

### 4. `arena_lookup_try_get` can infinite-loop on a full table (`:82`)
`while (true)` only exits when it finds a non-ACTIVE slot or a hash match. If the table is
ever 100% ACTIVE (no empty sentinel) and the key is absent, it spins forever. The 0.7 load
factor *should* guarantee an empty slot — but only if resize (bug #1/#2) actually works.
Defense in depth: bound the probe count by `capacity`.

---

## Design feedback (account.h)

### `const` struct members make your out-params unwritable
```c
struct xl_account_snapshot {
    int32_t balance;                    // not const
    const enum xl_acct_normality norm;  // const
    const xl_smallstr64 name;           // const
    const xl_smallstr128 desc;          // const
};
```
A snapshot is filled via `..._snapshot(..., struct xl_account_snapshot * out)`. To populate
it the implementation must assign to its members — but `const` members can only be set by an
initializer at object creation, not by later assignment (`out->name = ...` is ill-formed).
You'll either fight the compiler with casts (UB if the object is genuinely const) or be
unable to write the snapshot at all. **Const-ness of a *copy* should live with the
variable, not baked into the type.** Drop the member `const`s; let callers declare
`const struct xl_account_snapshot snap`.

### View vs. snapshot inconsistency
In `xl_account_view`, `balance`/`name`/`desc` are *pointers* into the arena (that's what
gives the "lifetime guarantee"), but `norm` is a plain value `const enum xl_acct_normality`.
A copied enum has no lifetime relationship to the arena — it defeats the view's purpose and
is inconsistent with its siblings. Make it `const enum xl_acct_normality * norm`.

### Smaller notes
- `try_get_account_name_by_handle(..., const xl_smallstr64 * out_str)` — an *out* parameter
  that's `const` can't be written to. Same category as the snapshot issue.
- Doc cleanup (`@return`, ERRORS sections) is a real improvement — keep doing this. A few
  ERRORS blocks are still empty stubs; fill or remove them so they don't rot.

---

## Style / project-fit

Your CLAUDE.md lists "prioritize readability over cleverness." Several spots trade
readability for branchless micro-tricks that the compiler would do anyway:
- `c += 32 * ((c <= 'Z') & (c >= 'A'));` (hash lowercasing)
- `index *= (index < table->capacity);` (probe wraparound)
- `hash & (table_capacity - (table_capacity != UINT16_MAX))` (mask)

None of these are on a measured hot path yet. Write the obvious `if`/`% capacity` version,
*then* reach for tricks only when a profile says this function matters. You're explicitly
here to learn profiling — so let measurement, not intuition, justify cleverness. (The
`UINT16_MAX` edge case in the mask is genuinely subtle; a one-line comment on *why* it's
there would help future-you.)

Naming: files are kebab-case (`arena-lookup-table.c`) but `account_store.{c,h}` /
`account-arena` mix kebab and snake. Pick one convention for filenames.

---

## Scorecard

| Area | Grade | Note |
|------|-------|------|
| Architecture / data model | A− | SoA arena + separate index is the right call |
| Correctness | C | Resize path is broken; latent UB on uninit memory |
| Testing discipline | C+ | Good structure, but assertions don't cover the failure |
| API design | B− | Solid handle/view/snapshot idea; `const` placement hurts it |
| Readability vs. constraints | B | Cleverness creeping in ahead of profiling |

**Headline:** the bones are good and the design is more sophisticated than I'd expect at
this level. The theme of the week is **make your tests prove the property, and treat
uninitialized memory as a hard error, not a probability.** Fix the resize + rehash, recolor
the tests to catch it, switch to `calloc`, and this becomes a genuinely solid component.

Homework for the week is in `.claude/homework/20-06-2026/QUESTIONS.md`.

---

## Session log — 2026-06-20 (Sat, Opus)

**Type:** Planning / infrastructure. No production code written this session.

**Done:**
- Stood up the mentoring workflow: `/session-start` + `/session-end` skills (fixed from
  broken `SKILLS.md`/underscore names to valid `SKILL.md`/kebab-case), `journal/`,
  `homework/20-06-2026/QUESTIONS.md`, `LEARNING_PLAN.md`, and `HANDOFF.md` for
  cross-session continuity.
- Locked the `account_store` design as **ADR 0001** (`docs/decisions/`): slot-only handles,
  heap-allocated opaque store, store-driven lockstep growth, no account-closing in v1,
  normality stored in the SoA tag column.
- Reviewed the week's lookup-table + arena work (see review above).
- Committed + pushed setup (`df3821c`); also pushed 4 earlier local-only commits.

**Decisions for the record:** see ADR 0001 (D1–D5, invariants I1–I2).

**Mistakes / notes:** none in code (none written). Two doc-consistency actions are still
open and owned by the student — `architecture.md` §4 (remove closed-accounts step) and the
I1 invariant comment in `account.c`. Tracked in `HANDOFF.md`.

**Carry-over:** `account.h`/`account.c` edits remain unstaged WIP. Next session starts on
the lookup-table rehash fix (homework Q1), per `HANDOFF.md`.

---

## Session log — 2026-06-21 (Sun, Opus)

**Type:** SoA design discussion. No production code written. Student requested we revisit
account-SoA assumptions before `account_store` cements them.

**Decisions reached (raw material for ADR 0002 — student to author; supersedes 0001 D4/D5,
enforces D1):**
- Drop the `generation` column (aligns arena with D1's slot-only handles; was dead weight).
- Drop the `tags` byte entirely. Net-worth class is subsumed by the future type/bucket
  dimension; the reserved status bit is dropped — a *conscious* reversal of D4, accepting a
  future format migration if account deactivation ever returns.
- Normality → its own 1-byte `enum` column (7 wasted bits accepted). Supersedes D5; stored
  directly, *not* derived — knowingly interim, will be superseded by buckets.
- Descriptions → `xl_smallstr128` string pool, `descs` column becomes a `uint32_t` index.
  Names stay inline in the SoA this week (keeps lookup table / I2 untouched). Store the
  index, not a pointer — pool reallocs on grow. Snapshots copy out (safe); desc views must
  resolve through `pool_base + idx`.
- Capacity policy: grow-on-demand, geometric (doubling), sized to the DB row count at
  startup load. Full uint16-range pre-alloc rejected (MBs to hold hundreds of rows). Linear
  (+N) growth rejected: O(n²) total copy work and breaks power-of-two lockstep with the
  lookup table (D3). Chunked/segmented arena reserved as a profiled-need future option.
- Future, reserved (new ADR when built): type/account **buckets** — partition by account
  type; normality becomes derived; supersedes the normality column and D1's flat-slot handle.

**Teaching points this session:** (1) "out of band" only helps a *dense* attribute if it's
*derived* — a side table for a dense field is a pessimization; positional encoding (buckets)
is the only real win. (2) Growth-policy reasoning: linear amortization is O(n²), not safer;
per-session insert count ≠ lifetime growth policy. (3) Contradictory premises — "not
perf-critical" defeats a "resize-latency" worry; can't optimize for both.

**Mistakes / notes:** no code, so no code mistakes. The two open doc actions from 2026-06-20
(`architecture.md` §4, the I1 comment) are now partly subsumed by ADR 0002 but still owed.

**Carry-over / next session:** write ADR 0002 from the decision list (HANDOFF action #1),
then the lookup-table rehash fix (Q1–Q3) — unchanged by today's decisions since names stay
inline. `account_store` stub bumped to next week. Plans reconciled in `HANDOFF.md` and
`LEARNING_PLAN.md`.
