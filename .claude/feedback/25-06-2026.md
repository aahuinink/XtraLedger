# Feedback — 2026-06-25 (Thu)

Reviewed: `src/xlcore/memory/arena-lookup-table.c` (partial calloc patch, uncommitted),
`include/xlcore/account.h` (redesign, uncommitted), `tests/internal/lookup-table.c`
(resize-survival test, uncommitted). All three are carry-overs from the 2026-06-24 session.

## `arena-lookup-table.c` — the patch left a compile error

Line 227: `free(old_entry_array)` — `old_entry_array` is not declared anywhere in the function.
This doesn't compile. The `memcpy` was removed but the `old_entry_array` variable it relied on
was never created. The file is in a state that's worse than the `memcpy` version because at
least that compiled.

This is exactly why the HANDOFF decision was "rewrite, not patch." Stop trying to fix the patch;
delete `increase_capacity_if_needed`'s resize branch and write it cleanly.

## `arena-lookup-table.c` — calloc vs sentinel (partial credit)

The `calloc` call is there, which eliminates the UB. But `ENTRY_STATE_ACTIVE = 0xF364` is still
defined and used throughout. This is inconsistent: you `calloc` to zero-initialize, so `state == 0`
IS your empty sentinel now — but the code still uses `0xF364`. Either:
- Remove `ENTRY_STATE_ACTIVE` and replace all checks with `state != 0` / `state = 1`, OR
- Leave the macro but rename it to something that reflects "occupied" (not "active") and set it
  to any non-zero value (1 is fine, no need for a magic number anymore).

The magic number `0xF364` is now pointless — `calloc` guarantees zero init, so ANY non-zero value
works as "occupied." Pick 1. The comment about UB on line 163-164 becomes stale; remove it.

## `account.h` — Q4 fix still not applied

`xl_account_snapshot` fields at lines 29–31 still have `const`. Your Q4 answer correctly
diagnosed this four days ago. The understanding is there; it just wasn't applied. Apply it before
starting any accessor implementation.

`xl_account_view.norm` at line 37 is still a value (`const enum xl_acct_normality norm`). Every
other field in the view is a pointer. Make it `const enum xl_acct_normality * norm` to be
consistent and so the view can actually point into the SoA column.

## `tests/internal/lookup-table.c` — the test is correct

Lines 55–63 re-query all 8 keys after the resize. That's exactly what Q2 asked for. Do not touch
this test. The rewrite succeeds when this test passes.

## Pattern to break: "I understand it" ≠ "it's done"

Q4 fix, the dead `get_starting_entry` prototype (still there, line 29), the `try_get` unbound
probe (still there, line 82) — all three were identified two sessions ago. You understood them.
None are landed. Commit working code before starting the next problem.

## Today's priority order

1. **Commit the working tree as-is** (homework answers, account.h design, broken partial patch,
   test). Get it into git; then the rewrite is a fresh diff against a known baseline.
2. **Rewrite `increase_capacity_if_needed`**: save old pointer + capacity first, allocate new
   zeroed array, set new capacity, reinsert all live old entries using stored hash, free old.
3. **Bound the probe loop** in `try_get`: after `capacity` probes with no match, return -1.
4. **Delete the dead prototype** (`get_starting_entry`, line 29).
5. **Apply the Q4 fix** to `account.h`: remove `const` from snapshot fields, make `view.norm` a
   pointer.

The rewrite passes when `lookup_table_init_and_deinit_succeeds` passes end-to-end.
